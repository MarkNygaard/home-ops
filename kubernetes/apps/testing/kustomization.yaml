---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: testing

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./k6-operator/ks.yaml
  # Active load test — one at a time. Merging fires it; revert/remove to stop.
  # Switch tests by swapping which trigger line is uncommented:
  #   inventory-stress: ./k6-operator/testrun/ks.yaml
  - ./k6-operator/campaign/ks.yaml
