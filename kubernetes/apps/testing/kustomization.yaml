---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: testing

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./k6-operator/ks.yaml
  # Activates the inventory-stress-v2 load test (3000 req/s, 5m, dev2).
  # Remove this line + push to stop and clean up the run.
  - ./k6-operator/testrun/ks.yaml
