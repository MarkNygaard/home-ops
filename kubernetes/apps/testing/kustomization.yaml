---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: testing

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./k6-operator/ks.yaml
  # No load test active. To run one, uncomment its trigger and push — merging
  # fires it; remove the line again to stop and prune the run:
  #   inventory-stress: ./k6-operator/testrun/ks.yaml
  #   campaign:         ./k6-operator/campaign/ks.yaml
